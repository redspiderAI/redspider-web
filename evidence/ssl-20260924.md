# hzai.tech SSL 更新记录

更新时间：2026-09-24 09:18（北京时间）。用户明确授权更新证书及测试 HTTPS。

- 来源：用户提供的 `hzai.tech_nginx.zip`。
- 包 SHA-256：`a4871a79191068f828a6c82ffef4de39bd7275771cb89c35b0e27999a050e0bf`。
- 已核对 SSH 别名 `zgyjserver` 与域名均对应 `124.223.188.111`。
- 新证书 SAN：`hzai.tech`、`www.hzai.tech`。
- 有效期：2026-09-24 08:00:00 至 2026-12-23 07:59:59（北京时间）。
- 证书 SHA-256 指纹：`32:E8:2C:3E:19:4E:50:B9:97:67:0C:BC:6B:4F:DF:B5:E3:EE:54:63:77:FD:BD:9C:F6:CB:F9:CE:9F:2F:C2:AB`。
- 私钥匹配、有效期、完整证书链及域名验证通过。
- 生效目录：`/opt/teenai-h5/releases/d78cfb0de3a23bbf4b652de29c75eceee49e2e70/secrets/tls/`。
- 旧证书备份：`/opt/teenai-h5/tls-backups/20260924-0118/`，目录权限 700，私钥权限 600。

更新保留了单文件 Docker bind mount 的 inode。完整写入两份文件后执行 `nginx -t` 和 `nginx -s reload`；网关容器 ID、镜像和业务路由均未改变，健康状态为 healthy。失败回滚保护未触发。

## 验证

服务器 curl 开启证书验证，以下四个路径返回 200、`ssl_verify_result=0`：

- `https://hzai.tech/`
- `https://hzai.tech/AiCampHomePage/`
- `https://hzai.tech/AiCampSignUp/`
- `https://hzai.tech/AiCampSignUp/api/health`

本机 Python 使用系统信任库、`CERT_REQUIRED` 和主机名验证，独立完成 TLS 1.3 握手，读回相同指纹、有效期；以上四个 URL 全部返回 200。没有跳过证书或主机名验证。

本机 Windows curl 默认 Schannel 检查另报 `0x80092013`（撤销服务器不可达）；这是该客户端撤销查询失败。上述独立 TLS 验证和服务器 curl 结果均通过，不将该错误隐去，也不把关闭校验当成成功依据。

此记录仅证明 TLS 更新与页面/健康接口连通，不代表报名或支付流程的人工验收。

## www DNS 的既有问题

本机解析 `www.hzai.tech` 得到 `124.223.188.111` 与 `106.52.254.226` 两个地址。前者的 443/TLS 校验通过且已读到新证书，后者的 443 拒绝连接。证书已覆盖 www 域名，但 DNS 旧地址仍需清理，不能声称 www 的所有解析路径正常。本次未修改 DNS。
